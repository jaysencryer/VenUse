import Modal from "./Modal";

const BookSlots = ({ avail, booked, bookings }) => {
    console.log(bookings[0].room);
    return (
        <div>
                <div>Something new</div>
        </div>
    );
};

export default BookSlots;
